apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fibo-nest.fullname" . }}-test-connection"
  labels:
    {{- include "fibo-nest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fibo-nest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
