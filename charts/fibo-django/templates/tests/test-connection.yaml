apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fibo-django.fullname" . }}-test-connection"
  labels:
    {{- include "fibo-django.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fibo-django.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
