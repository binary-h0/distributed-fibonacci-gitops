apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fibo-next.fullname" . }}-test-connection"
  labels:
    {{- include "fibo-next.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fibo-next.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
