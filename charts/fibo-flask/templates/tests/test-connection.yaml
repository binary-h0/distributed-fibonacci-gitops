apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fibo-flask.fullname" . }}-test-connection"
  labels:
    {{- include "fibo-flask.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fibo-flask.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
