apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fibo-express.fullname" . }}-test-connection"
  labels:
    {{- include "fibo-express.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fibo-express.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
